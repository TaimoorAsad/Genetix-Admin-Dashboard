"use client";

import AppContentEditor from "@/components/AppContentEditor";

export default function TestimonialsPage() {
  return (
    <AppContentEditor
      section="testimonials"
      title="Testimonial information"
      description="Manage content shown in the Testimonials tab in the app. Add text, image URLs, or YouTube/video links. Blocks are shown in order."
    />
  );
}
